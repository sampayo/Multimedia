<div class="seccion2">
	<% if @apro %>
	<%= render('users/style') %>
	<%= render('users/menuDer') %>
	<%else%>
	<%= render ('form') %>
	<%end%>
	<div class="span-14">
		<h3>Buscar</h3>
		<div >
			<p id="notice">
				<%= notice %>
			</p>
<%= render ('users/tablas') %>
		</div>
	</div>
</div>